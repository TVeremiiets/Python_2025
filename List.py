import itertools #recomendation to look

some_list =[55,44]
some_string = 'jkefrihi'

list_from_string = list
pass

#elements_in_some_string = len(some_string)
#elements_in_some_list = len(some_list)
#slice_data = list_from_string [0:2]

#All
list_with_data = [22 ,25, 66, 0, 1]
all_true = all(list_with_data)
at_list_one = any(list_with_data)

maximum = max(some_list)
minimum = min(some_list)
summa = sum(some_list , start=10)
some_str_list = ['dfgrthd', 'dfjhbefgbkhb','jhuy', [] ]
max_str = max(some_str_list,key=len)
min_str = min(some_str_list,key=len)